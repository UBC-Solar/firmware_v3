name: TEL Continuous Integration Pipeline

jobs:
  build_tel:
    name: Build TEL
    uses: xanderhendriks/action-build-stm32cubeide@v13.0
    with:
      project-path: 'components/tel/'
      project-target: 'tel'